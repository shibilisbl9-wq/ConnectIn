# WarnMark

A signal-red X beside a warning sentence; the only place red appears.

**Markup:** `p.ci-warn` with the sentence as text. The X is drawn by CSS in `signal`.

- Do: always with words; the red alone is not the message.
- Don't: decorative red (petals, hearts, red backgrounds).
