# Pointer

The supporting line under the headline, marked by the gold triangle; replaces », >>> and every other chevron.

**Markup:** `p.ci-pointer`. Max 2 lines (max-width 520px on canvas).

- Do: say what the swipe delivers ("Here's what you need sorted").
- Don't: stack more than one pointer on a hero. Two pointers = two posts.
